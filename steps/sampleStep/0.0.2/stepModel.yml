configuration:
  foo:
    type: String
    validate:
      isRegex: "[A-Za-z_][A-Za-z_]+"
  boo:
    type: uddt
userDefinedDataType:
  - type: uddt
    configuration:
      bar:
        type: Int
        validate:
          max: 10
      baz:
        type: String
        validate:
          isUppercase: true


